# one to launch them all!

launch:

- include:
    file: "$(find-pkg-share can_bridge)/launch/can_bridge.yaml"
    
- include:
    file: "$(find-pkg-share mqtt_bridge)/launch/mqtt_bridge.yaml"

- include:
    file: "$(find-pkg-share quad_rover_kinematics)/launch/qrk.yaml"